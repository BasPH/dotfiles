" Display settings
set number          " Show line numbers
set ruler           " Show cursor position
set mouse=a         " Mouse support
set incsearch       " Search as characters are entered
set hlsearch        " Highlight search results
set showmode        " Show insert/replace/visual mode
syntax enable       " Colored syntax
set t_Co=256        " Enable 256 colors
set cursorline      " Highlight current line
highlight CursorLine ctermbg=8  " Highlight settings for current line

" Editing setting
set tabstop=4       " Number of spaces per tab (reading mode)
set softtabstop=4   " Number of spaces per tab (editing mode)
set expandtab       " Convert tabs into space
